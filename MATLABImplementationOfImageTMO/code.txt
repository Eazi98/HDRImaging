public double[] FromAppMethod()
    {
        File file = new File("test"); //for HDRtofloatarray
//        pixelArrayText = "";
        try {
            hdrtofloatarray = new HDRtofloatarray(file);
        } catch (IOException e) {
            e.printStackTrace();
        }
        double[][][] pixelArray = hdrtofloatarray.getPixelArray();
        int length = hdrtofloatarray.getHeight();
        int width = hdrtofloatarray.getWidth();
        int sizeOfImage = width*length*3;
        //+2 for adding information into the array for width and length
        double[] pixelDoubleArray = new double[sizeOfImage+2];

        pixelDoubleArray[0] = length;
        pixelDoubleArray[1] =  width;

        for (int i = 0; i < pixelArray.length; i++)
             for (int j = 0; j < pixelArray[i].length; j++)
                 for (int k = 0; k < pixelArray[i][j].length; k++){
                     //TODO:
                     //Find out how to get index
                     pixelDoubleArray[i*pixelArray[i].length+j+k*(pixelArray.length * pixelArray[i].length)+2] = pixelArray[i][j][k];
                 }


        //add length and width to start of array list
        pixelArrayTextArray.add(0, (double) width);
        pixelArrayTextArray.add(0, (double) length);


        //TODO: Convert pixelArray to a readable string
        return pixelDoubleArray;
    }